#include<stdio.h> //this will add the library studio input output to the program which is written and then compile(basically include the library when running the code)
int main()
{
    printf("Hey this is C programming");
    return 0;
}

/*how to run a .c file without using run command
go to the terminal and type
gcc filename.c -o filename (this will compile the file and turn it into an exe file)
.\filename.exe(this will invoke the exe file and run the program in the file)
*/



